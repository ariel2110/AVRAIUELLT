# FastAPI framework
fastapi==0.115.0

# ASGI server — [standard] pulls in uvloop + httptools for maximum performance
uvicorn[standard]==0.32.0

# SQLAlchemy 2 with async support
sqlalchemy[asyncio]==2.0.36

# asyncpg — high-performance async PostgreSQL driver (used by SQLAlchemy)
asyncpg==0.30.0

# Pydantic v2 — data validation and settings
pydantic==2.9.2

# Rate limiting middleware for FastAPI/Starlette (in-memory, no Redis needed)
slowapi==0.1.9

# Load .env files into os.environ
python-dotenv==1.0.1
